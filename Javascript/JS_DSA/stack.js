let stack = [];

// Push elements onto the stack
stack.push(1);
stack.push(2);

// Pop the top element from the stack
let poppedItem = stack.pop();

console.log(poppedItem); // Output: 2 (Last-in, first-out)
